---
version: null
meta:
  environment: jvm
  toolchain:
    name: maven
    version: 8
build:
  artifacts:
    target/*.jar: /
  args: spring-boot:repackage
run:
  jarName: store-0.0.1-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: 8080
